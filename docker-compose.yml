services:
  dashboard:
    build: .
    ports:
      - "8501:8501"
    env_file:
      - .env
    volumes:
      - ./sentinel.db:/app/sentinel.db

  # Optional: run against a fully local model, no external API at all.
  # Uncomment and set TARGET_PROVIDER=ollama / OLLAMA_BASE_URL=http://ollama:11434/v1 in .env
  # ollama:
  #   image: ollama/ollama
  #   ports:
  #     - "11434:11434"
  #   volumes:
  #     - ollama_data:/root/.ollama

# volumes:
#   ollama_data:
